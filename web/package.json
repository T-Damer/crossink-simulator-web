{
  "name": "@crosspoint/xteink-browser-emulator",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "lint": "biome check ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.9",
    "typescript": "^7.0.2",
    "vite": "^8.2.2"
  }
}
